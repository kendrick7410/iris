# Iris runtime dependencies — pinned for reproducibility

anthropic==0.49.0
click==8.1.3
httpx==0.28.1
matplotlib==3.7.1
python-dotenv==0.21.1

# Tests
pytest==8.3.3

# Comext trade ingestion — used when COMEXT_DATA_PATH is set
pandas==2.2.0
pyarrow==14.0.2
